// import { transparentize } from 'polished';
import styled from 'styled-components';
import CounterWrapper from './CounterWrapper';

export const CartPanel = styled(CounterWrapper)`
  position: relative;
  top: 10px;
`;
